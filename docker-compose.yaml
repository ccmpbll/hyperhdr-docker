services:
  hyperhdr:
    image: ccmpbll/hyperhdr:latest
    container_name: hyperhdr
    network_mode: host
    devices:
      - /dev/video0:/dev/video0
    environment:
      - TZ=America/New_York
    volumes:
      - ./config:/config
    restart: unless-stopped
